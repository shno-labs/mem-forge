/**
 * The local OSS deployment is single-workspace. The label is a non-interactive
 * affordance today; real switching arrives with the auth/provisioning sibling
 * surface and will replace this module then.
 */
export const ACTIVE_WORKSPACE_NAME = "Local workspace";
